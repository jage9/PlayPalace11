# Hangin' with Friends

game-name-hangin_with_friends = Hangin' with Friends
hwf-prompt-enter-word = Enter a word using your rack:
hwf-prompt-guess-word = Enter your full-word guess:

# Options
hwf-set-starting-balloons = Starting balloons: { $count }
hwf-enter-starting-balloons = Enter starting balloons:
hwf-option-changed-starting-balloons = Starting balloons changed to { $count }.

hwf-set-rack-size = Rack size: { $count }
hwf-enter-rack-size = Enter rack size:
hwf-option-changed-rack-size = Rack size changed to { $count }.

hwf-set-min-word-length = Min word length: { $count }
hwf-enter-min-word-length = Enter minimum word length:
hwf-option-changed-min-word-length = Minimum word length changed to { $count }.

hwf-set-max-word-length = Max word length: { $count }
hwf-enter-max-word-length = Enter maximum word length:
hwf-option-changed-max-word-length = Maximum word length changed to { $count }.

hwf-set-base-wrong-guesses = Base wrong guesses: { $count }
hwf-enter-base-wrong-guesses = Enter base wrong guesses:
hwf-option-changed-base-wrong-guesses = Base wrong guesses changed to { $count }.

hwf-set-max-rounds = Max rounds: { $count }
hwf-enter-max-rounds = Enter max rounds (0 for no limit):
hwf-option-changed-max-rounds = Max rounds changed to { $count }.

hwf-set-max-score = Max score: { $score }
hwf-enter-max-score = Enter max score (0 for no limit):
hwf-option-changed-max-score = Max score changed to { $score }.

hwf-set-default-bot-difficulty = Default bot difficulty: { $mode }
hwf-select-default-bot-difficulty = Select default bot difficulty:
hwf-option-changed-default-bot-difficulty = Default bot difficulty changed to { $mode }.

hwf-set-dictionary-mode = Dictionary mode: { $mode }
hwf-select-dictionary-mode = Select dictionary mode:
hwf-option-changed-dictionary-mode = Dictionary mode changed to { $mode }.

hwf-toggle-full-word-guess = Full-word guess: { $enabled }
hwf-option-changed-full-word-guess = Full-word guess changed to { $enabled }.

hwf-toggle-spectators-see-all-actions = Spectators see all actions: { $enabled }
hwf-option-changed-spectators-see-all-actions = Spectators see all actions changed to { $enabled }.

hwf-set-bot-guess-aggression = Bot guess aggression: { $mode }
hwf-select-bot-guess-aggression = Select bot guess aggression:
hwf-option-changed-bot-guess-aggression = Bot guess aggression changed to { $mode }.

# Choices
hwf-bot-difficulty-easy = Easy
hwf-bot-difficulty-medium = Medium
hwf-bot-difficulty-hard = Hard
hwf-bot-difficulty-extreme = Extreme

hwf-dictionary-mode-strict = Strict
hwf-dictionary-mode-rack-only = Rack only
hwf-dictionary-mode-off = Off

hwf-bot-guess-safe = Safe
hwf-bot-guess-balanced = Balanced
hwf-bot-guess-risky = Risky

# Per-bot control
hwf-set-bot-difficulty = Set bot difficulty
hwf-select-bot-difficulty = Select bot and difficulty:

# Errors
hwf-error-min-length-greater-than-max = Minimum word length cannot be greater than maximum word length.
hwf-error-rack-smaller-than-max-length = Rack size cannot be smaller than maximum word length.
